import React from 'react';
import { motion } from 'framer-motion';

const Merge = () => {
    return (
        <motion.div className="merge" layout>
            <h3>Merging Branches...</h3>
        </motion.div>
    );
};

export default Merge;
